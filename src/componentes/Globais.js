import React from "react";

export default class Globais  {       // Veja que aqui é uma classe normal JS e não um Componente de Classe
    static canal = "adilson canal";   // Por isso não é obrigatório constructor, super, render, etc
    static curso = "adilson curso";
    static ano = 2042;
}

